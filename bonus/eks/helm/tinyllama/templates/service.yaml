apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-tinyllama
spec:
  selector:
    app: tinyllama
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: {{ .Values.service.type }}
